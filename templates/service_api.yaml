apiVersion: v1
kind: Service
metadata:
  name: {{ include "teanote.fullname" . }}-api
  labels:
    {{- include "teanote.labels" . | nindent 4 }}
    app: api
spec:
  type: {{ .Values.service.api.type }}
  ports:
    - port: {{ .Values.service.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "teanote.selectorLabels" . | nindent 4 }}
    app: api